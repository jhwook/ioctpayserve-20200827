
db.operations.findOne({raw:true,where:{key_:'MIN_BALANCE_TO_INVOKE_TX_ON_CHANGE',subkey_:currency0}}).then(async respoper0=>{respoper=respoper0;    console.log(respoper)  })


jdata={username:'33332222',sitename:'WWIFI',currency0:'WIFI',amount0:}
}
exchange
jdata= {
  sitename: 'wwifi',
  username: 'user01',
  currency0: 'WIFI',
  amount0: 0.4
}

array=[{k:1,v:2}, {k:4,v:5}]
const conva2j=(array,keyin)=>{
json = { ...array };
json = Object.assign({}, array);
json = array.reduce((json, value, key) => { json[keyin] = value; return json; }, {})
return json
}
